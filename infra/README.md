# Infrastructure

This directory will contain reviewed infrastructure-as-code, deployment environments, network policies, observability configuration and recovery automation.

Real secrets and generated state must never be committed. Production changes require review, automated validation, a rollback path and least-privilege deployment credentials.
