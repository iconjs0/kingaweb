# Shared Contracts

This package will contain versioned schemas for API payloads, scan jobs, observations, findings and audit events.

Contracts should be machine-validated, backward-compatible where promised and free of business logic. Security-relevant changes require review and an Architecture Decision Record when they alter a trust boundary.
