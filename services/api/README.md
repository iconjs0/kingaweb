# Control-Plane API

This service will own identity integration, workspaces, authorization, assets, findings, reports, subscriptions, audit events and job scheduling.

It may request bounded scans only after policy and target-verification checks. It does not perform outbound scanning in the API process.
